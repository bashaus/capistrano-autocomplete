h1. capistrano-autocomplete

h3. 1. Install bash-completion package

Homebrew
<pre>brew install bash-completion</pre>

Macports
<pre>sudo port install bash-completion</pre>

Yum
<pre>yum install bash-completion</pre>

Apt Get
<pre>apt-get install bash-completion</pre>

h3. 2. Install the 'capistrano' auto-complete script into /usr/local/etc/bash_completion.d

h3. 3. Relaunch bash/terminal